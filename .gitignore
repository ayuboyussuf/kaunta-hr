# dependencies
node_modules/
# builds
dist/
.next/
out/
# env
.env
.env.local
.env*.local
# misc
.DS_Store
*.log
.vercel
next-env.d.ts
# TypeScript incremental build cache. Rewritten by every tsc and next build,
# so tracking it put a meaningless diff in commits that had nothing to do with
# the frontend.
*.tsbuildinfo
